# راهنمای نوشتن در وبلاگ

برای انتشار یک نوشته‌ی جدید:

1. داخل `src/content/posts` یک پوشه با نام انگلیسی و کوتاه بساز.
2. داخل آن فایل `index.mdx` بساز.
3. این frontmatter را در ابتدای فایل قرار بده:

```md
---
title: 'عنوان نوشته'
description: 'توضیح کوتاه نوشته'
pubDate: 2026-09-22
tags: ['توسعه وب']
---
```

4. متن فارسی را بعد از خط دوم `---` بنویس.
5. تغییرات را commit و push کن. GitHub Pages بعد از build، نوشته را در `/posts` نمایش می‌دهد.

برای نمایش نوشته در ابتدای صفحه، مقدار `pinned: true` را به frontmatter اضافه کن. برای پیش‌نویس موقت هم از `draft: true` استفاده کن.
