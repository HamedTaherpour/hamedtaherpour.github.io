# Optional GitHub OAuth App values for Gitalk comments
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Used only by the local build script to fetch the private/authenticated
# contribution calendar. In GitHub Actions, use a repository secret instead.
GITHUB_CONTRIBUTIONS_TOKEN=your-github-contributions-token
